/**
 * Placeholder until the real types are generated from the running database:
 *
 *   npm run db:types   (requires `supabase start` or a linked project)
 *
 * Do not hand-edit generated output once that command has been run —
 * regenerate it instead so it stays in sync with supabase/migrations/.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
